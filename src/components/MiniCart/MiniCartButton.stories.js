import React from "react";
import { storiesOf } from "@storybook/react";
import MiniCartButton from "./MiniCartButton";

storiesOf("Components|MiniCart/MiniCartButton").add("Default", () => (
  <MiniCartButton />
));
